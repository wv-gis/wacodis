import { IsoplethenDatasetSchema } from './isoplethen-dataset-schema.model';

describe('IsoplethenDatasetSchema', () => {
  it('should create an instance', () => {
    expect(new IsoplethenDatasetSchema()).toBeTruthy();
  });
});
